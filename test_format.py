def badly_formatted_function(x, y, z):
    if x > 0:
        result = x + y * z
        return result
    else:
        return None


def another_function(a, b):
    data = [1, 2, 3, 4, 5]
    for i in data:
        print(f"Value: {i}")
    return a + b
